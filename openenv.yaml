name: openenv_api_orchestration

env:
  tasks:
    - name: complete_workflow
      description: Complete all API calls successfully
      grader:
        type: python
        entry_point: grade_complete_workflow

    - name: no_errors
      description: No errors during execution
      grader:
        type: python
        entry_point: grade_no_errors

    - name: efficient_execution
      description: Finish in minimal steps
      grader:
        type: python
        entry_point: grade_efficiency
